# Product Contract

Canonical product contract lives at:

```text
docs/product/PRODUCT_CONTRACT.md
```

This file exists as a stable top-level pointer because agent prompts and human
tasks commonly refer to `docs/PRODUCT_CONTRACT.md`.

Agents must read the canonical file before changing user-visible behavior.
