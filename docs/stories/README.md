# Stories

Create a story packet when work is larger than a tiny direct patch or when the
behavior should become durable product knowledge.

Use `docs/templates/story.md`.

Story IDs should use `US-###-short-name.md`.
